\begin{circuitikz}[european,american voltages]
    \ctikzset{voltage/distance from node=0.99}
    \ctikzset{voltage/shift=3}

    \draw[<->] (0,0.05)--(0,3.95);
    \node[left] at (0,2) {$U$};

    \ctikzset{voltage/american plus=\textcolor{red}{\tiny $+Q_n$}}
    \ctikzset{voltage/american minus=\textcolor{blue}{\tiny $-Q_n$}}
    \draw (0,4) to[short,o-]
    ++ (4.5,0) ++ (1,0) to[short]
    ++ (1.0,0) to[C,l=$C_n$,v=$~$] 
    ++ (0,-4)  to[short] 
    ++ (-1.0,0)
    ++ (-1,0) to[short,-o] ++(-4.5,0);

    \ctikzset{voltage/american plus=\textcolor{red}{\tiny $+Q_1$}}
    \ctikzset{voltage/american minus=\textcolor{blue}{\tiny $-Q_1$}}
    \draw (1.25,4) to[C,l=$C_1$,v=$~$] ++ (0,-4);

    \ctikzset{voltage/american plus=\textcolor{red}{\tiny $+Q_2$}}
    \ctikzset{voltage/american minus=\textcolor{blue}{\tiny $-Q_2$}}
    \draw (3.5,4) to[C,l=$C_2$,v=$~$] ++ (0,-4);

    \node at (5,0) {$\cdots$};
    \node at (5,2) {$\cdots$};
    \node at (5,4) {$\cdots$};

    \clip (-0.5,-0.2) rectangle (7.5,4.2);
\end{circuitikz}
