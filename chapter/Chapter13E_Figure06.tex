\begin{tikzpicture}[scale=1.5]

\tikzset{frame/.style={black!40!white,ultra thin}}
\tikzset{wall/.style={ultra thick}}
\tikzset{lig/.style={postaction={decorate},very thin}}
\tikzset{aux/.style={postaction={decorate},ultra thin,gray}}

\draw[frame] (-3.5,-3.5) rectangle (5.5,3.5);

\clip (-3.5,-3.5) rectangle (5.5,3.5);

\path (-2,0) coordinate(S);

\path
($(S)+(0,+1.5)$) coordinate (S1)
($(S)+(0,-1.5)$) coordinate (S2);

\path (-1,0) coordinate(W);

\draw[wall] ($(W)+(0,+3)$)--($(W)+(0,+1.5)$)
    coordinate(W1) node[above left] {$A$};
\draw[wall] ($(W)+(0,-3)$)--($(W)+(0,-1.5)$)
    coordinate(W2) node[below left] {$B$};

\path (+4.25,0) coordinate (L);

\draw[wall,name path=ls] ($(L)+(0,+3)$)--($(L)+(0,-3)$);

\path ($(L)!0.5!(W)$) coordinate (O);

\draw[dotted,name path=ox] ($(O)+(0,+3)$)--($(O)+(0,-3)$);

\path ($(W2)!0.25!(W1)$) coordinate (Wtemp);

\path[name path=l2] (Wtemp)--($(Wtemp)!4!(O)$);

\path[
    name intersections={of=ls and l2,by=T2}] (T2) node[below right] {$P$};

\draw[thin,gray,->]
    ($(W1)!0.75!(W2)$) coordinate (TEMP1)
    ($(TEMP1)!0!(O)$) -- ($(TEMP1)!2.3!(O)$) node[above]{\small 光轴};

\draw[thin,->]
    ($(W)!-0.65!(O)$) -- ($(W)!2.3!(O)$) coordinate (Phi1);

\begin{scope}[decoration={markings,
    mark=at position 0.7 with {\arrow{latex}}}]

    \foreach \x in {0,0.125,...,1}
    {
        \draw[lig] ($(S1)!\x!(S2)$) -- ($(W1)!\x!(W2)$) coordinate (Wx);
        
        \draw[lig] (Wx) -- ++($(O)-(Wtemp)$) -- (T2);
    }
\end{scope}

\draw  (W) ++($(O)-(Wtemp)$) coordinate (Phi2)
pic["$\phi$",angle radius=1cm,angle eccentricity=1.5,draw=black] {angle=Phi1--W--Phi2};

\path (W2) ++ ($(O)-(Wtemp)$) coordinate (Wprd);



\path ($(W2)!(W1)!(Wprd)$) coordinate (Plen1) node[below right] {$H$};
\path ($(Plen1)!0.5!(W1)$) coordinate (Plen2);
\path ($(Plen2)-(W1)$) coordinate (Pl);

\draw[gray]
pic[angle radius=0.1cm,draw=black] {right angle=W1--Plen1--W2};

\draw[gray]
pic["$\phi$",angle radius=0.7cm,angle eccentricity=1.3,draw=gray] {angle=W2--W1--Plen1};

\foreach \x/\t in {0/0,0.25/1,0.50/2,0.75/3,1/4}
{
    \draw[aux] ($(W1)!\x!(W2)$) coordinate (Wprx) -- ($(W2)!(Wprx)!(Wprd)$) coordinate(Wpa\t) -- ++(Pl) coordinate(Wpb\t);
}

\foreach \t in {0,1,2,3}
{
    \draw[aux,<->] ($(Wpa\t)!0.75!(Wpb\t)$)-- node[below,rotate=15] {$\tiny \frac{\lambda}{2}$} ($(Wpa\fpeval{1+\t})!0.75!(Wpb\fpeval{1+\t})$);
}

\draw[thick,red,opacity=0.3]  (W1)--($(W1)!0.25!(W2)$);
\draw[thick,red,opacity=0.3]  ($(W1)!0.50!(W2)$)--($(W1)!0.75!(W2)$);
\draw[thick,blue,opacity=0.3] (W2)--($(W1)!0.75!(W2)$);
\draw[thick,blue,opacity=0.3] ($(W1)!0.50!(W2)$)--($(W1)!0.25!(W2)$);

\draw[thick,fill=black,fill opacity=0.1] 
($(O)+(0,+2.5)$)
to[out=-105,in=105]
($(O)+(0,-2.5)$)
to[out=75,in=-75]
($(O)+(0,+2.5)$);

\draw[aux] ($(W1)+(-1.8,0)$) coordinate (W1') -- ($(W1)+(-2.2,0)$) coordinate (W1'');
\draw[aux] ($(W2)+(-1.8,0)$) coordinate (W2') -- ($(W2)+(-2.2,0)$) coordinate (W2'');

\draw[aux,<->] ($(W1')!0.5!(W1'')$) -- node[left] {$a$} ($(W2')!0.5!(W2'')$);

\path
    (W1) node[point]{}
    (W2) node[point]{}
    (O) node[point]{}
    (T2) node[point]{};

\end{tikzpicture}
